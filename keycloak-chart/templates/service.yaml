apiVersion: v1
kind: Service
metadata:
  name: keycloak-service
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 8080
    name: http
  selector:
    app: keycloak
